name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [8.x, 10.x, 12.x]

    steps:
    - uses: sergioramos/yarn-actions/install@master
  with:
    runs-on: ubuntu-latest
